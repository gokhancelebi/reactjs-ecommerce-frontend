import Container from "../../UI/Container";
import Header from "../../UI/Header";

const Orders = (props) => {

    return (
        <Container>
            <Header />
            <h1>orders</h1>
        </Container>
    )
}

export default Orders;