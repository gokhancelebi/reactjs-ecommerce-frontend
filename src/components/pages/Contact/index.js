import Container from "../../UI/Container";
import Header from "../../UI/Header";

const Contact = (props) => {

    return (
        <Container>
            <Header />
            <h1>Contact Page</h1>
        </Container>
    )
}

export default Contact;